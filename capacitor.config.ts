import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cricket-platform-mobile-app',
  webDir: 'www'
};

export default config;
